# Production Environment Configuration
# This file should be used for production deployments

# API Configuration
# Set this to your production API server URL if you have one
# Leave empty to disable server sync (use localStorage sync only)
VITE_API_URL=

# App Configuration
VITE_APP_NAME=Система учета техники на складе

# Note: In production on Vercel, the app will work with localStorage sync only
# If you want to enable server sync, set VITE_API_URL to your production server
